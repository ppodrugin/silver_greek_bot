# 🚀 Деплой на Render (Пошаговая инструкция)

## Почему Render?

- ✅ **Бесплатно** для Background Worker
- ✅ Не засыпает (в отличие от Web Service)
- ✅ Простой деплой через GitHub
- ✅ Автоматический деплой при каждом push

## Шаг 1: Подготовка кода

Убедитесь, что код загружен на GitHub:

```bash
cd telegram_bot
git add .
git commit -m "Ready for deployment"
git push
```

## Шаг 2: Создайте аккаунт на Render

1. Перейдите на https://render.com
2. Нажмите "Get Started for Free"
3. Войдите через GitHub (рекомендуется)

## Шаг 3: Создайте Background Worker

**⚠️ ВАЖНО: Используйте Background Worker, НЕ Web Service!**

1. В Dashboard нажмите "New +"
2. Выберите **"Background Worker"** (не Web Service!)
3. Нажмите "Connect account" если нужно
4. Выберите ваш GitHub репозиторий с ботом

## Шаг 4: Настройте Background Worker

Заполните форму:

### Основные настройки:
- **Name**: `greek-telegram-bot` (или любое имя)
- **Region**: Выберите ближайший (например, Frankfurt)
- **Branch**: `main` (или ваша основная ветка)
- **Root Directory**: Оставьте пустым (если бот в корне репозитория)

### Команды:
- **Environment**: `Python 3`
- **Build Command**: `pip install -r requirements.txt`
- **Start Command**: `python3 bot.py`

### План:
- **Plan**: `Free` (выбрано по умолчанию)

## Шаг 5: Добавьте переменные окружения

Перед созданием сервиса, прокрутите вниз до раздела "Environment Variables" и добавьте:

### Обязательные:
- **Key**: `TELEGRAM_BOT_TOKEN`
- **Value**: Ваш токен от @BotFather

### Опциональные:
- **Key**: `OPENAI_API_KEY`
- **Value**: Ваш OpenAI API ключ (если используете)

## Шаг 6: Создайте сервис

1. Нажмите "Create Background Worker"
2. Render начнет деплой
3. Дождитесь завершения (2-5 минут)

## Шаг 7: Проверьте логи

1. После создания сервиса откройте его
2. Перейдите в раздел "Logs"
3. Должны увидеть:
   ```
   ✅ База данных SQLite инициализирована
   ✅ Супер-пользователь 799341043 добавлен
   Бот запущен...
   Application started
   ```

## Шаг 8: Проверьте работу бота

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Если бот отвечает - всё работает! ✅

## Автоматический деплой

Render автоматически деплоит при каждом push в GitHub:
1. Сделайте изменения в коде
2. `git commit` и `git push`
3. Render автоматически перезапустит бота с новым кодом

## Просмотр логов

В любой момент можно посмотреть логи:
1. Откройте ваш Background Worker в Render Dashboard
2. Перейдите в раздел "Logs"
3. Видите все логи бота в реальном времени

## Обновление переменных окружения

Если нужно изменить переменные окружения:
1. Откройте ваш Background Worker
2. Перейдите в "Environment"
3. Измените или добавьте переменные
4. Render автоматически перезапустит сервис

## Проблемы?

### Бот не отвечает
1. Проверьте логи в Render
2. Убедитесь, что `TELEGRAM_BOT_TOKEN` установлен правильно
3. Проверьте, что токен действителен

### Ошибки при деплое
1. Проверьте логи сборки (Build Logs)
2. Убедитесь, что `requirements.txt` содержит все зависимости
3. Проверьте, что `bot.py` находится в корне репозитория

### База данных не работает
1. SQLite файл создастся автоматически
2. На бесплатном плане файлы сохраняются между перезапусками
3. Если нужна постоянная БД, используйте PostgreSQL (платно)

## Полезные ссылки

- Render Dashboard: https://dashboard.render.com
- Документация Render: https://render.com/docs
- Поддержка: https://render.com/docs/support

