# 🚀 Быстрый старт: Деплой бота

## Самый простой способ: Railway

### 1. Подготовка репозитория

```bash
cd telegram_bot
git init
git add .
git commit -m "Initial commit"
```

### 2. Создайте репозиторий на GitHub
1. Перейдите на https://github.com/new
2. Создайте новый репозиторий (например, `greek-telegram-bot`)
3. НЕ добавляйте README, .gitignore или лицензию (они уже есть)
4. Скопируйте команды для push

### 3. Загрузите код на GitHub
```bash
git remote add origin https://github.com/ваш-username/greek-telegram-bot.git
git branch -M main
git push -u origin main
```

### 4. Деплой на Railway
1. Перейдите на https://railway.app
2. Войдите через GitHub
3. Нажмите "New Project" → "Deploy from GitHub repo"
4. Выберите ваш репозиторий
5. Railway автоматически определит Python проект

### 5. Настройте переменные окружения
В проекте Railway:
1. Перейдите в "Variables"
2. Добавьте:
   - `TELEGRAM_BOT_TOKEN` = ваш токен от @BotFather
   - `OPENAI_API_KEY` = ваш OpenAI ключ (опционально)

### 6. Готово! 🎉
Бот автоматически запустится. Проверьте логи в разделе "Deployments".

## Альтернатива: Render

### 1. Подготовка (та же, что выше)

### 2. Деплой на Render
1. Перейдите на https://render.com
2. Войдите через GitHub
3. "New +" → "Background Worker" (не Web Service!)
4. Подключите ваш репозиторий
5. Настройки:
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python3 bot.py`
6. Добавьте переменные окружения (как в Railway)
7. "Create Background Worker"

## Важно! ⚠️

1. **НЕ коммитьте `.env` файл** - он уже в `.gitignore`
2. **База данных**: SQLite файл создастся автоматически на хостинге
3. **Логи**: Все логи доступны в панели управления хостинга

## Проверка работы

После деплоя отправьте боту `/start` в Telegram. Если бот отвечает - всё работает! ✅

## Проблемы?

1. Проверьте логи на хостинге
2. Убедитесь, что переменные окружения установлены
3. Проверьте, что `requirements.txt` содержит все зависимости

